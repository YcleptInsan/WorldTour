@if (@Model.Count == 0)
{
  <p>No bands have been added yet!</p>
}
else
{
  <h1>Here is your list of bands:</h1>
  <ul>
    @foreach (var band in Model)
    {

      <li><a href="/bands/@band.GetId()">@band.GetName()</a></li>

    }
  </ul>
}
<p><a href="/bands/new">Add a new band</a></p>
<p><a href="/">Return home</a></p>
